


//Retrieve Transaction Info (Can be called by all Level CX Agents)

//Modify Transaction Information(Can Only be called by Level 1 CX Agents)

//Delete Transaction (Can Only be called by Admin)

//Verify Transaction Details(Can be called by all CX Agents)

//Recall Transaction(Can only be called by Leevel 1 CX Agents)

//Chargeback Transaction(Can only be called by Level 1 CX Agents)